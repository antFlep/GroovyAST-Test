allprojects {
	
	apply plugin: 'groovy'
	
	repositories {
		jcenter()
	}
	
	dependencies {
		compile group: 'org.codehaus.groovy', name: 'groovy-all', version: '2.4.17'
	}
	
}

dependencies{
	compile project(':annotation-transform')
}